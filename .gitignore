# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out


# Node_Modules folder. Takes up space that isn't required and can be created on node install

binds_tf2/node_modules/*